import { Router } from "express";

const router = Router()

router.post('/', () => { })
router.post('/:hotelId/rooms', () => { })
router.get('/', () => { })
router.get('/:hotelId', () => { })

export default router